char  c;
char *str;
long anInt;

int main() {
  anInt = 377;
  printint(anInt);
  c= '\n'; printint(c);

  for (str= "Hello world\n"; *str != 0; str= str + 1) {
    printchar(*str);
  }
  return(0);
}